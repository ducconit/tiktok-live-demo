# Dev image — Bun + Vite dev server (compose mount ./dashboard:/app)
FROM oven/bun:1

WORKDIR /app

# Cache layer cho node_modules
COPY package.json bun.lock* ./
RUN bun install

EXPOSE 5173

ENTRYPOINT ["bun", "run", "dev", "--host", "0.0.0.0"]