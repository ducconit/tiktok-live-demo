# TikTok Bar server config (Go)
# Copy to .env and fill in your values.

# Server port
PORT=3001

# Euler Stream sign API key (OPTIONAL — anonymous signing works but is rate-limited)
# https://www.eulerstream.com
SIGN_API_KEY=

# Optional: custom sign server base URL (overrides Euler Stream)
SIGN_SERVER_URL=
